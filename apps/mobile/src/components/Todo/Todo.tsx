import { Input } from "@rneui/themed";
import { View } from "react-native";

export const Todo = () => {
    return (
        <View>
            <Input label={"Votre future todo"} />
        </View>
    );
};
